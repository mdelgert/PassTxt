#pragma once

#include <unordered_map>
#include <string>
#include <FastLED.h> // Include FastLED for CRGB

// Declare the color map as an external variable
extern const std::unordered_map<std::string, CRGB> colorMap;